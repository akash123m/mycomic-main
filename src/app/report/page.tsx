"use client";
import { useState } from "react";
export default function ReportPage(){const [sent,setSent]=useState(false);return <main className="legal-page"><p className="section-label">Safety & rights</p><h1>Report content</h1><p>Report copyright concerns, unsafe material, impersonation, or another issue. Include the exact series or chapter URL.</p>{sent?<div className="report-success">Report received. We’ll review it as soon as possible.</div>:<form className="report-form" onSubmit={async(event)=>{event.preventDefault();const form=new FormData(event.currentTarget);const response=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),email:form.get("email"),message:`Content report — ${form.get("url")}\nReason: ${form.get("reason")}\n${form.get("details")}`})});if(response.ok)setSent(true)}}><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Series or chapter URL<input name="url" type="url" required/></label><label>Reason<select name="reason"><option>Copyright or ownership</option><option>Unsafe or unlawful content</option><option>Impersonation</option><option>Other</option></select></label><label>Details<textarea name="details" rows={5} required/></label><button type="submit">Submit report</button></form>}</main>}
