"use client"; import { useState } from "react"; import { useMutation,useQuery,useQueryClient } from "@tanstack/react-query"; import { Mail,Download,CheckCircle2,Eye } from "lucide-react"; import SectionHeader from "@/components/ui/SectionHeader"; import { requestJson } from "@/lib/api";
type Item={id:string;name:string;email:string;message:string;status:string;createdAt:string};type Page={data:Item[];nextCursor:string|null;total:number};
export default function Contacts(){const qc=useQueryClient(),[status,setStatus]=useState("ALL"),[q,setQ]=useState(""),[cursor,setCursor]=useState<string|undefined>();const key=["admin","contacts",status,q,cursor];const{data,isLoading,isError}=useQuery({queryKey:key,queryFn:()=>requestJson<Page>(`/api/admin/contacts?status=${status}&q=${encodeURIComponent(q)}${cursor?`&cursor=${cursor}`:""}`)});const update=useMutation({mutationFn:({id,status}:{id:string;status:string})=>requestJson(`/api/admin/contacts/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status})}),onSuccess:()=>qc.invalidateQueries({queryKey:["admin","contacts"]})});return <div><SectionHeader title="Contact Inbox" subtitle="Review and resolve messages from visitors"/><div className="admin-tools"><input placeholder="Search messages…" value={q} onChange={e=>{setQ(e.target.value);setCursor(undefined)}}/><select value={status} onChange={e=>{setStatus(e.target.value);setCursor(undefined)}}>{["ALL","NEW","READ","RESOLVED"].map(x=><option key={x}>{x}</option>)}</select><a href={`/api/admin/contacts?status=${status}&q=${encodeURIComponent(q)}&format=csv`}><Download size={15}/>Export CSV</a></div>{isLoading?<p>Loading messages…</p>:isError?<p className="error">Unable to load messages.</p>:!data?.data.length?<p className="admin-empty">No messages found.</p>:<div className="admin-list">{data.data.map(x=><article key={x.id}><div><span className={`status ${x.status.toLowerCase()}`}>{x.status}</span><h3>{x.name}</h3><a href={`mailto:${x.email}`}>{x.email}</a><p>{x.message}</p><small>{new Date(x.createdAt).toLocaleString()}</small></div><div className="row-actions">{x.status==="NEW"&&<button onClick={()=>update.mutate({id:x.id,status:"READ"})}><Eye size={15}/>Mark read</button>}{x.status!=="RESOLVED"&&<button onClick={()=>update.mutate({id:x.id,status:"RESOLVED"})}><CheckCircle2 size={15}/>Resolve</button>}</div></article>)}</div>}<div className="pager"><span>{data?.total??0} messages</span>{data?.nextCursor&&<button onClick={()=>setCursor(data.nextCursor!)}>Next page →</button>}</div><AdminStyles/></div>}
function AdminStyles(){return <style jsx global>{`.admin-tools{display:flex;gap:9px;flex-wrap:wrap;margin-bottom:20px}.admin-tools input,.admin-tools select{padding:10px 12px;border-radius:9px;background:var(--color-bg-elevated);border:1px solid var(--color-border);color:var(--color-text-primary)}.admin-tools input{min-width:min(100%,260px);flex:1}.admin-tools a,.row-actions button,.pager button{display:inline-flex;align-items:center;gap:6px;padding:9px 12px;border-radius:9px;background:var(--color-bg-elevated);border:1px solid var(--color-border);color:var(--color-text-primary);text-decoration:none}.admin-list{display:grid;gap:11px}.admin-list article{display:flex;justify-content:space-between;gap:18px;padding:18px;border-radius:14px;background:var(--color-bg-surface);border:1px solid var(--color-border)}.admin-list h3{margin:7px 0 2px}.admin-list p{margin:10px 0;color:var(--color-text-secondary);white-space:pre-wrap}.admin-list small{color:var(--color-text-tertiary)}.row-actions{display:flex;align-items:flex-start;gap:7px;flex-wrap:wrap}.status{font-size:10px;font-weight:800;color:var(--color-primary)}.pager{display:flex;justify-content:space-between;margin-top:18px}.admin-empty{padding:35px;text-align:center;background:var(--color-bg-surface);border:1px dashed var(--color-border);border-radius:14px}.error{color:var(--color-error)}@media(max-width:650px){.admin-list article{flex-direction:column}.row-actions button{flex:1;justify-content:center}}`}</style>}
